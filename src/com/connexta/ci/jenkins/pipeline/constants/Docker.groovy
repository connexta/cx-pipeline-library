package com.connexta.ci.jenkins.pipeline.constants

class Docker {
    static final String DOCKERD_INSECURE_REG_OPTION = "--insecure-registry"
    static final String DOCKERD_DEFAULT_LOG = "/var/log/dockerd.log"
}
